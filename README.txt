In this lab, we handled HTTP request

We loaded and explored Simple APIs, such as:
*   RandomUser API, using `RandomUser()` Python library
*   Fruitvice API, using `requests` Python library

We carried out Webscraping in Python
